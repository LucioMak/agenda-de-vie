{
  "name": "agenda-de-vie",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --format=iife --target=es2019 --outfile=www/app.js"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "@capacitor/local-notifications": "^8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "esbuild": "^0.28.2"
  }
}
